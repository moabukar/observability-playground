groups:
  - name: infra_alerts
    rules:
      - alert: AppDown
        expr: up{job="app"} == 0
        for: 30s
        labels:
          severity: critical
          service: app
        annotations:
          summary: "Application is down"
          description: "App service has been down for more than 30 seconds"