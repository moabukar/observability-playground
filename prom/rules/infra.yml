groups:
  - name: infra.rules
    rules:
      - alert: AppDown
        expr: up{job="app"} == 0
        for: 1m
        labels: { severity: page }
        annotations:
          summary: "App target down"
